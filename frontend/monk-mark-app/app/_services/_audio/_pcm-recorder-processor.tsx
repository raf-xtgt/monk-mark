/**
 * PCM Recorder Processor for React Native
 * 
 * Note: This file is a placeholder for compatibility with the web implementation.
 * In React Native, audio processing is handled natively by expo-audio-stream
 * in _audio-recorder-service.tsx, so we don't need a separate worklet processor.
 * 
 * Web Audio API worklets are not available in React Native.
 * Instead, we use expo-audio-stream which provides native audio recording
 * with automatic PCM conversion.
 */

export const PCMRecorderProcessor = {
  // Placeholder for web compatibility
  // Actual implementation is in _audio-recorder-service.tsx
};
